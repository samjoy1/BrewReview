import { hasUserReviewed, hasUserVoted } from "../utilities";

function postReview (username, beer_id) { // username: the username of the user leaving the review | beer_id: the id of the beer the review is for
    if (!hasUserReviewed(username, beer_id)) {

    }
}

function postBeer () {

}

function createNewUser () {

}

function createNewCategory () {
    
}